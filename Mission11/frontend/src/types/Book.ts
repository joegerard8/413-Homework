export interface Book {
    bookId: number,
    title: string,
    author: string,
    publisher: string,
    isbn: string,
    classificiation: string, 
    category: string,
    pageCount: number, 
    price: number,
}